package staticc.kotlin

class StaticExample {

    /**
     * Similar a static methods and variables.
     * Only one per class.
     * */
    companion object {
        val PI:Double = Math.PI
    }
}